# Sync plan — FEAT-0086 Multi-file extension workbench

Started: 2026-08-08T13:49:38.269Z
Snapshot state: drifted
Drift summary: 0 added, 5 modified, 0 removed

## AC-2/AC-3 — one editor with path-owned drafts

Remove the tab-slot model from the workbench session. A sidebar click directly
selects and loads that path in the existing CodeMirror editor. Preserve the
current extension/path-keyed draft map and mtime guard so switching files and
returning restores the correct unsaved text without cross-file writes.

Files: `src/workbench-main.ts`, `e2e/extensions.spec.ts`.

## AC-4 — remove tab chrome and open-file state

Delete the tab strip, trailing new-tab action, tab rendering/activation/closing
code, and all tab CSS. Collapse the editor panel grid so its first row is the
existing breadcrumb/actions toolbar. Add static and browser assertions that no
tab or new-tab control is rendered while file selection still works.

Files: `workbench.html`, `src/workbench-main.ts`, `src/styles.css`,
`src/workbench.test.ts`, `e2e/extensions.spec.ts`.

## AC-5/AC-7 — refresh and honest section chrome

Simplify refresh, rename, and delete reconciliation to selected-path state only.
Keep the file list/editor usable after invalidation and retain the chevron-free
always-visible Extensions and Files headings.

Files: `src/workbench-main.ts`, `src/workbench.test.ts`.

## Documentation

Update milestone, roadmap, extension guide, architecture note, and decision log
to state that the workbench deliberately uses one editor without tabs.

Files: `milestones/M41.md`, `ROADMAP.md`, `docs/extensions.md`,
`docs/excavation/extension-workbench/01-architecture.md`, `DECISIONS.md`.

## Verification

- `specman validate --strict`
- `npm test -- --run src/workbench.test.ts`
- `npm test -- --run`
- `npm run build`
- `npm run e2e -- e2e/extensions.spec.ts --project=chromium`
- `git diff --check`

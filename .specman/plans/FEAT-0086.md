# Sync plan — FEAT-0086 Multi-file extension workbench

SpecMan sync was blocked by the intentionally uncommitted M39 base in this
read-only-git worktree. This plan records the phase-first implementation
contract before code changes.

## AC-1/AC-4 — safe file tree and diagnostics

Extend script storage to enumerate only supported manifest/JavaScript/JSON files
deterministically and report malformed manifests or unsupported paths without
blocking unrelated scripts.

Files: src/script-storage.ts, src/script-manifest.ts, src/script-storage.test.ts.

## AC-2/AC-3 — multi-file editor

Replace the single-source modal path with a workbench file selection model,
language-aware CodeMirror loading, per-file dirty state, and an explicit
discard/save decision before switching dirty files.

Files: src/script-editor.ts, src/workbench.ts, src/workbench-main.ts,
src/workbench.test.ts.

## AC-5 — missing attachment state

Keep the UI actionable when the vault is missing or unavailable and avoid
creating metadata directories during a failed attach.

Files: src/workbench.ts, src/workbench-main.ts, src/workbench.test.ts.

## Verification

- `specman validate --strict`
- `npm test -- --run src/script-storage.test.ts src/script-editor.test.ts src/workbench.test.ts`
- `npm test -- --run`
- `npm run build`

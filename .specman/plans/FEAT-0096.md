# Sync plan — FEAT-0096 Preflight and durable phase-ledger checker

Started: 2026-08-11T13:15:34.776Z
Snapshot state: new
Drift summary: 7 added (whole spec)

## AC-1 (added): Given a clean checkout with tracked `AGENTS.md`, the required documents,
  the project-local mapping, a runnable `specman status --verbose`, and a valid
  ledger, when `npm run workflow:check -- preflight --milestone milestones/M45.md`
  runs, then it exits zero and reports a successful preflight.

Approach: Collect read-only repository observations in the CLI, pass them to a
  pure evaluator, and print a stable success report only when every check is
  valid. Keep the milestone path explicit rather than inferring it from chat.

Files:
- `scripts/workflow-check.mjs`
- `scripts/workflow-check.test.mjs`
- `package.json`

## AC-2 (added): Given a preflight observation with one or more missing required paths,
  when it is evaluated, then every missing path is reported with a stable error
  code and the command exits nonzero without creating or editing files.

Approach: Represent checks as `{ code, path, message }` values and aggregate all
  missing paths before returning. The CLI only reads and reports; the fixture
  tests call the evaluator directly and verify no fixture mutation.

Files:
- `scripts/workflow-check.mjs`
- `scripts/workflow-check.test.mjs`

## AC-3 (added): Given a worktree with uncommitted or untracked changes, when preflight
  evaluates ownership, then it reports `dirty-worktree` and blocks the phase
  transition instead of silently accepting the state.

Approach: Treat any porcelain line beyond the branch header as dirty ownership.
  Keep this check separate from content validation so a clean but semantically
  invalid ledger cannot be mistaken for a clean worktree.

Files:
- `scripts/workflow-check.mjs`
- `scripts/workflow-check.test.mjs`

## AC-4 (added): Given a milestone ledger missing `Current phase`, `Last completed gate`,
  or `Next action`, when it is parsed, then the specific missing ledger field is
  reported and the phase remains blocked.

Approach: Parse the three bold ledger labels with anchored line patterns and
  return field-specific errors; do not repair or normalize a malformed ledger.

Files:
- `scripts/workflow-check.mjs`
- `scripts/workflow-check.test.mjs`
- `milestones/M45.md`

## AC-5 (added): Given an unavailable or failing `specman status --verbose`, when
  preflight runs, then it reports `specman-unavailable` with the command output
  and exits nonzero without claiming spec state.

Approach: Run `specman status --verbose` as a child command, preserve its stderr
  or exit information in the observation, and map every nonzero/launch failure
  to `specman-unavailable` without interpreting partial output as state.

Files:
- `scripts/workflow-check.mjs`
- `scripts/workflow-check.test.mjs`

## AC-6 (added): Given a valid preflight observation, when the checker completes, then it
  performs no writes and returns a structured success containing the milestone
  path, ledger fields, and observed checks.

Approach: Keep the evaluator pure and make the CLI print the returned structured
  observation. Tests snapshot the directory entries and ledger bytes around a
  successful run to prove the checker is read-only.

Files:
- `scripts/workflow-check.mjs`
- `scripts/workflow-check.test.mjs`

## AC-7 (added): Given the package scripts and checker source, when a fresh checkout runs
  the documented command, then the command resolves to the checker without a
  machine-local alias or global configuration.

Approach: Add one package script that invokes the checked-in Node entrypoint;
  tests invoke the same package command and assert the stable report.

Files:
- `package.json`
- `scripts/workflow-check.mjs`
- `scripts/workflow-check.test.mjs`

## Verification

- `npm run workflow:test`
- `npm run workflow:check -- preflight --milestone milestones/M45.md`
- `npm test -- --run`
- `npm run build`
- `git diff --check`

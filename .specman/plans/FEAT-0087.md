# Sync plan — FEAT-0087 Extension lifecycle and safe file operations

Started: 2026-08-08T13:14:17.229Z
Snapshot state: drifted
Drift summary: 0 added, 2 modified, 0 removed

## AC-2 — selected-file delete action

Add a compact delete button beside the Files `+`. Disable it without a selected
file. On invocation, reuse the in-app modal shell in confirmation mode; required
manifest/entry files show a local protected-file error and remain untouched.
For companions, confirm against the selected path and last-seen mtime, then
remove its tabs/draft and select the next usable slot. Write the browser test
first.

Files: `workbench.html`, `src/workbench-main.ts`, `src/styles.css`,
`src/workbench.test.ts`, `e2e/extensions.spec.ts`.

## AC-4 — selected-extension delete action

Add a compact delete button beside the Extensions `+`. Confirm the selected id
and then call the existing safe subtree deletion. If the id is enabled, remove
it from `.brulion.json`; refresh the dropdown and active tab/file state after
success. Keep enable/disable outside the workbench and do not restore the old
options panel. Write the browser test first.

Files: `workbench.html`, `src/workbench-main.ts`, `src/styles.css`,
`src/workbench.test.ts`, `e2e/extensions.spec.ts`, `docs/extensions.md`,
`ROADMAP.md`, `milestones/M41.md`, `DECISIONS.md`.

## Verification

- `specman validate --strict`
- `npm test -- --run src/workbench.test.ts src/script-storage.test.ts`
- `npm test -- --run`
- `npm run build`
- `npm run e2e -- e2e/extensions.spec.ts --project=chromium`
- `git diff --check`

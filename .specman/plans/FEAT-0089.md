# Sync plan — FEAT-0089 Allowlisted extension command icons

SpecMan sync was blocked by the intentionally uncommitted M39 base in this
read-only-git worktree. This plan records the phase-first implementation
contract before code changes.

## AC-1/AC-2/AC-3 — icon boundary

Extend command registration with an optional bounded icon name, resolve only
allowlisted Lucide nodes, and default all missing or invalid values to puzzle
without accepting SVG markup.

Files: src/extension-host.ts, src/extension-host.test.ts, src/extension-icons.ts,
src/extension-icons.test.ts.

## AC-4 — compact action bar

Ensure resolved extension actions use existing icon rendering and compact
accessible toolbar buttons without changing palette invocation.

Files: src/ui.ts, src/actions.ts, src/main.ts, src/extension-icons.ts,
e2e/extensions.spec.ts.

## AC-5 — isolation

Keep icon parsing pure and independent from runner execution so invalid metadata
cannot break command registration or another extension.

Files: src/extension-host.ts, src/extension-icons.ts,
src/extension-host.test.ts.

## Verification

- `specman validate --strict`
- `npm test -- --run src/extension-icons.test.ts src/extension-host.test.ts src/actions.test.ts`
- `npm test -- --run`
- `npm run build`

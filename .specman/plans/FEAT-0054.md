# Sync plan — FEAT-0054 folder switch in settings and svg gear icon

Started: 2026-06-24T06:45:37.533Z
Snapshot state: new
Drift summary: 0 added (whole spec)

## AC-1 (added) — Folder section names the open folder

Approach: In `src/settings-modal.ts`, add a Folder section appended after the Vim
row (so it sits at the bottom of the dialog). It is a labeled row ("Folder") whose
control is a small container holding a folder-name `<span>` and a "Switch folder…"
button. Extend `SettingsModalHandlers` with `getFolderName: () => string`; `seed()`
sets the name span's text from it on every open/sync. The host supplies the open
folder's name from the directory handle.

Files: src/settings-modal.ts, src/main.ts

## AC-2 (added) — "Switch folder…" runs the open flow

Approach: Extend `SettingsModalHandlers` with `onSwitchFolder: () => void`. The
"Switch folder…" button's click handler invokes it. In `src/main.ts`, wire
`onSwitchFolder: () => void openFolder(resumeButton, openNote)` — the exact flow the
removed header `#reopen-folder` button drove (`wireOpenFolder(reopenButton, …)`),
reusing `openFolder` (imported from `./ui`) → `openNote` (reloads notes +
`.brulion.json`) → `saveFolder`. No new open logic.

Files: src/settings-modal.ts, src/main.ts

## AC-3 (added) — Switching dismisses the modal

Approach: The "Switch folder…" click handler calls the modal's internal `close()`
*before* `handlers.onSwitchFolder()`, so the modal is gone before the native picker
appears (and stays closed whether or not a folder is picked). Keeping the dismiss
inside the modal makes it unit-testable without the FSA.

Files: src/settings-modal.ts

## AC-4 (added) — Cancelling the picker changes nothing

Approach: Already guaranteed by the reused flow: `openFolder` no-ops on a dismissed
picker (`if (!dir) return`, src/ui.ts:20) — no `onOpen`, no `saveFolder`. The modal
is closed (AC-3) but the open folder/workspace is untouched. No new code; covered by
updating the existing e2e that exercises a real picker stub.

Files: (none — behavior of existing openFolder)

## AC-5 (added) — Header has no folder button or indicator

Approach: Remove the `<button id="reopen-folder">` from `index.html`. In
`src/main.ts` drop the `reopenButton` query and the
`wireOpenFolder(reopenButton, …)` call. In `src/ui.ts` remove `reopen` from
`WorkspaceRefs` and the `refs.reopen.hidden = false` line in `showWorkspace`, and
drop `reopen` from the refs object passed in `main.ts`. No folder-name element is
added to the header. Update `e2e/welcome.spec.ts` (clicks `#reopen-folder`) to
switch folders via the settings modal's "Switch folder…" button instead.

Files: index.html, src/main.ts, src/ui.ts, e2e/welcome.spec.ts

## AC-6 (added) — Settings entry point is an inline SVG gear

Approach: Replace the `⚙` text inside `#open-settings` in `index.html` with an
inline SVG gear (Feather-style: a centre `<circle>` + the gear `<path>`,
`fill="none" stroke="currentColor"`, `aria-hidden="true"`, ~18px). Add CSS in
`src/styles.css` so the button centers the glyph (`display: inline-flex; align-items:
center`) and sizes the SVG. The button keeps its id, type, `aria-label`/`title`.

Files: index.html, src/styles.css

## AC-7 (added) — Gear behavior and shortcut unchanged

Approach: No behavioral change — `openSettingsEl.addEventListener("click", …)` and
the `Ctrl/Cmd+,` capture-phase handler (src/main.ts) are untouched; only the
button's inner glyph changes. The `aria-label="Settings"` and the platform shortcut
title stay. Confirmed by the existing settings-modal e2e still opening via the gear.

Files: (none — verified, no change)

## AC-8 (added) — Vim control shows its toggle shortcut

Approach: In `src/settings-modal.ts`, wrap the Vim checkbox and a `<kbd>` chip in
the Vim row's control. The chip shows the platform-correct chord computed from
`navigator.platform` (`⌘;` on Mac, else `Ctrl+;`) — the same check `main.ts` uses
for its other shortcut hints — with a `title` naming what it toggles. Hint only; no
change to the toggle or the `Ctrl/Cmd+;` binding (which lives in `main.ts`).

Files: src/settings-modal.ts, src/styles.css

## Tests

- src/settings-modal.test.ts — new cases: the Folder section renders the name from
  `getFolderName`; clicking "Switch folder…" calls `onSwitchFolder` once **and**
  closes the modal (backdrop hidden); the name re-seeds on `sync()`.
- e2e/settings-modal.spec.ts — assert the Folder section shows the folder name and
  the gear button contains an `svg` (not the `⚙` text).
- e2e/welcome.spec.ts — fold the header-button switch into the modal switch path.
- src/settings-modal.test.ts — the Vim row shows a `Ctrl+;` / `⌘;` shortcut chip.

## Verification

- `npm run build`
- `npm test -- --run`
- `npm run e2e -- settings-modal welcome`

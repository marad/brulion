# Sync plan — FEAT-0106 Extension interaction authoring kit and browser validation

Started: 2026-08-13T11:42:38.633Z
Snapshot state: new
Drift summary: 6 added (whole spec)

## AC-1 (added): Given the released Authoring Kit, when it is listed or downloaded, then
  it contains deterministic selection/notification and dialog lifecycle
  examples with manifests declaring only the permissions they use, alongside
  the template, declarations, contract, API guide, and agent-facing files; the
  examples are not enabled implicitly.

Approach: Bump the kit version and add two ordinary example directories: one
for direction-aware selection plus a success notification, and one for
formatted alert/confirm/prompt handling. Import both manifests and sources into
the existing deterministic `authoring-kit.ts` bundle; keep the examples
explicitly disabled by leaving enablement in the user's vault settings.

Files:
- extension-kit/examples/selection-feedback/manifest.json
- extension-kit/examples/selection-feedback/main.js
- extension-kit/examples/dialog-lifecycle/manifest.json
- extension-kit/examples/dialog-lifecycle/main.js
- src/authoring-kit.ts
- src/authoring-kit.test.ts

## AC-2 (added): Given the declarations and machine-readable contract, when contract
  tests parse them, then every v1 method and permission has matching signatures,
  types, bounds, examples, and errors, including `{ anchor, head, text }`,
  `MessageContent`, prompt `string | null` versus accepted `""`, FIFO host
  modal behavior, `disposed`/`timeout`, stale-vault rejection, and no obsolete
  selection or prompt labels.

Approach: Preserve the additive API v1 method and permission inventory, but
make the contract's interaction errors/behavior explicit and add declaration
comments for raw UTF-16 selection, safe message fragments, prompt cancellation,
modal serialization, source disposal, stale vaults, and the human deadline.
Strengthen contract tests to assert those claims and the absence of old shapes.

Files:
- extension-kit/brulion-extension.d.ts
- extension-kit/api-contract.json
- src/extension-api-contract.test.ts
- src/script-manifest.test.ts

## AC-3 (added): Given a person or LLM agent reading the kit, when they follow its guide
  and examples, then it states JavaScript ESM/explicit enablement, least
  privilege, no DOM/CodeMirror/FSA/network/package/timer escape hatches,
  mtime-guarded Markdown writes, safe formatted rendering, and how to handle
  confirmation, cancellation, disposal, timeout, and unchanged-file behavior.

Approach: Expand the human API guide's interaction section with one complete
formatted-message example, selection direction and UTF-16 notes, prompt outcome
handling, queue/focus/disposal/timeout behavior, and a file-fidelity checklist.
Cross-link the README, AGENTS, LLM skill, and authoring prompt to the same
contract and examples without adding a second hand-maintained API definition.

Files:
- extension-kit/API.md
- extension-kit/README.md
- extension-kit/AGENTS.md
- extension-kit/llm-skill.md
- extension-kit/authoring-prompt.md

## AC-4 (added): Given JavaScript is disabled or the workbench/API reference is opened,
  when an author looks for the contract, declarations, guide, or interaction
  methods, then the static links, generated method cards, shared types, kit
  listing, and downloadable bundle all expose the same versioned content and
  include the interaction examples without drift.

Approach: Keep the existing progressive API page and build-time public copies;
add artifact identity/version and interaction/example assertions to the docs
and kit tests, and extend the workbench/browser checks for the new kit rows and
`dialogs.prompt`/`notifications.show` reference cards.

Files:
- src/api-docs.test.ts
- src/authoring-kit.test.ts
- e2e/extensions.spec.ts
- public/api.md
- public/api-contract.json
- public/brulion-extension.d.ts

## AC-5 (added): Given a real Chromium/OPFS vault with an explicitly enabled example,
  when its user-invoked command exercises selection/feedback or a dialog and
  the author opens the workbench/API reference, then the real iframe/RPC path,
  least-privilege manifest, rendered result, lifecycle error guidance, and
  static kit surfaces are observable while the active Markdown bytes remain
  unchanged.

Approach: Add a dedicated Chromium/OPFS authoring-kit flow using the same
selection-feedback example contract: assert its least-privilege permissions,
exercise the command through the real palette and host interaction surfaces,
open the workbench kit and API docs, and compare the note bytes before/after.
Keep dialog lifecycle and coded timeout/disposal coverage in FEAT-0105's
existing real-RPC suite; this phase validates that the published authoring
surface teaches and exposes it.

Files:
- e2e/extension-authoring-kit.spec.ts

## AC-6 (added): Given the complete FEAT-0106 artifact set, when targeted tests, full
  Vitest, `specman verify`, build, and browser validation run, then they pass
  with a clean source worktree and all M46 specs are `in-sync`; no production
  API capability, permission meaning, or file-format behavior changes.

Approach: Verify the deterministic kit/parser/documentation tests, run the
whole Vitest suite, build the static copies, run the dedicated and existing
extension Chromium suites against OPFS, then run `specman verify FEAT-0106`,
`specman validate`, and the final review loop before initial sealing.

Files:
- src/authoring-kit.test.ts
- src/extension-api-contract.test.ts
- src/api-docs.test.ts
- e2e/extension-authoring-kit.spec.ts
- e2e/extension-dialogs.spec.ts

## Verification

- `npm test -- --run src/authoring-kit.test.ts src/extension-api-contract.test.ts src/api-docs.test.ts src/script-manifest.test.ts`
- `npm run build`
- `npm run e2e -- e2e/extension-authoring-kit.spec.ts e2e/extension-dialogs.spec.ts e2e/extensions.spec.ts`
- `npm test -- --run`
- `specman verify FEAT-0106`
- `specman validate`

# Sync plan — FEAT-0088 Versioned extension authoring kit

Started: 2026-08-08T14:35:24.467Z
Snapshot state: drifted
Drift summary: 1 added, 0 modified, 0 removed

## AC-7 — syntax-highlighted API code

Reuse CodeMirror's JavaScript/TypeScript parser and the existing Lezer
`classHighlighter` to emit `tok-*` spans for the API declaration block and any
fenced code blocks in the reference. Extend the shared token CSS to cover the
standalone documentation page in both light and dark themes. Keep source in
`textContent`/span text nodes so copying and selection preserve exact bytes.

Files: `src/api-docs-main.ts`, `src/styles.css`, `e2e/extensions.spec.ts`.

## Verification

- `specman validate --strict`
- `npm test -- --run src/workbench.test.ts src/authoring-kit.test.ts`
- `npm test -- --run`
- `npm run build`
- `npm run e2e -- e2e/extensions.spec.ts --project=chromium`
- `git diff --check`

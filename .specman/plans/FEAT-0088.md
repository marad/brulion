# Sync plan — FEAT-0088 Versioned extension authoring kit

Started: 2026-08-08T14:20:49.536Z
Snapshot state: new
Drift summary: 6 added (whole spec)

## AC-1–AC-5 — retain the versioned kit source

Keep the existing deterministic Authoring Kit catalog, exact copy/download
behavior, template validation, and capability-limited example checks unchanged.

Files: `src/authoring-kit.ts`, `src/authoring-kit.test.ts`, `workbench.html`,
`src/workbench-main.ts`.

## AC-6 — standalone API documentation window

Expose an explicit API docs action in the workbench titlebar. Open a same-origin
static `api.html` page in a named popup window, with a fallback new-tab anchor
when popup opening is blocked. The page imports the API reference and declaration
source from the Authoring Kit rather than duplicating it, renders the Markdown
summary and declarations readably, and does not depend on vault attachment.

Files: `workbench.html`, `src/workbench-main.ts`, `api.html`,
`src/api-docs-main.ts`, `src/styles.css`, `vite.config.ts`, `src/workbench.test.ts`,
`e2e/extensions.spec.ts`.

## Documentation

Record that API documentation is a standalone read-only surface backed by the
same versioned kit bytes and update the workbench/authoring-kit architecture
notes if needed.

Files: `DECISIONS.md`, `docs/excavation/extension-workbench/01-architecture.md`,
`milestones/M41.md`, `ROADMAP.md`.

## Verification

- `specman validate --strict`
- `npm test -- --run src/workbench.test.ts src/authoring-kit.test.ts`
- `npm test -- --run`
- `npm run build`
- `npm run e2e -- e2e/extensions.spec.ts --project=chromium`
- `git diff --check`

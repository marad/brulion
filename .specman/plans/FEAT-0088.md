# Sync plan — FEAT-0088 Versioned extension authoring kit

SpecMan sync was blocked by the intentionally uncommitted M39 base in this
read-only-git worktree. This plan records the phase-first implementation
contract before code changes.

## AC-1/AC-2 — repository kit

Add one versioned repository kit source with a disabled template, declarations,
examples, API reference, AGENTS.md, LLM skill, and authoring prompt; validate
the template against the existing manifest contract.

Files: extension-kit/, src/authoring-kit.ts, src/authoring-kit.test.ts.

## AC-3/AC-4 — workbench distribution

Expose a deterministic kit file list, exact copy action, and browser download
action from the separate workbench.

Files: src/authoring-kit.ts, src/workbench-main.ts, src/workbench.test.ts,
e2e/extensions.spec.ts.

## AC-5 — API compatibility

Keep examples JavaScript-only and constrained to the declared API; test the kit
for forbidden package/network/timer assumptions.

Files: extension-kit/, src/authoring-kit.test.ts.

## Verification

- `specman validate --strict`
- `npm test -- --run src/authoring-kit.test.ts src/script-manifest.test.ts`
- `npm test -- --run`
- `npm run build`

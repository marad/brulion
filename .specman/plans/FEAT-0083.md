# Sync plan — FEAT-0083 Extension host commands and capability API

Started: 2026-08-12T14:07:28.970Z
Snapshot state: drifted
Drift summary: 1 added, 0 modified, 0 removed

## AC-6 (added): Given the injected `notes.list` callback returns valid, duplicate, and
  legacy paths that fail note-name validation, when an extension calls
  `notes.list()`, then the call resolves with only valid normalized paths,
  deduplicated as before. Given an invalid path passed directly to
  `notes.read`, `notes.write`, `notes.create`, `notes.delete`, `notes.move`, or a
  navigation path input, then that call still rejects before its callback runs.

Approach: Keep the throwing `notePath` validator unchanged for direct path
arguments. In the host-side `notes.list` loop, validate each app-side entry
independently, omit entries whose normalization or `.brulion` root check fails,
and preserve canonical normalization plus `Set`-based deduplication for valid
entries. Add a regression for mixed legacy paths and retain a direct invalid
`notes.read` rejection. Document the omission in the Authoring Kit source; the
build regenerates the checked-in public artifacts.

Files: `src/extension-host.ts`, `src/extension-host.test.ts`,
`extension-kit/API.md`, `extension-kit/api-contract.json`,
`public/api.md`, `public/api-contract.json`.

## Verification

- `specman validate --strict`
- `npm test -- --run src/extension-host.test.ts`
- `npm test -- --run`
- `npm run build`

# Sync plan — FEAT-0091 Extension navigation API contract and permission boundary

Started: 2026-08-11T09:12:04.349Z
Snapshot state: new
Drift summary: 6 added (whole spec)

## AC-1 (added): Given the checked-in API v1 contract, declarations, API reference, and
  host method inventory, when their navigation surface is enumerated, then all
  three contain `navigation.getActiveNote`, `navigation.openNote`, and
  `navigation.resolveLink` with matching result/type names and no undocumented
  navigation RPC method.

Approach: Extend the existing structured API contract and host method inventory with one navigation namespace, its public result/option types, and the two least-privilege permissions. Keep API version 1 and make API surfaces consume the same names.

Files:
- `src/extension-api-contract.ts`
- `src/extension-host.ts`
- `src/extension-api-contract.test.ts`
- `extension-kit/brulion-extension.d.ts`
- `extension-kit/api-contract.json`
- `extension-kit/API.md`
- `src/authoring-kit.ts`

## AC-2 (added): Given a manifest requesting `navigation:read`, when an extension calls
  `getActiveNote` or `resolveLink`, then the host permits those calls and does
  not grant `openNote`; given a manifest requesting `navigation:write`, when it
  calls `openNote`, then the host permits it; omitted permissions reject before
  the injected callback runs.

Approach: Add `navigation:read` and `navigation:write` to manifest validation and register host handlers with a narrow injected capability object. Enforce read/write permission ownership independently so a manifest can request exactly the needed operations.

Files:
- `src/script-manifest.ts`
- `src/extension-host.ts`
- `src/extension-host.test.ts`
- `src/script-manifest.test.ts`

## AC-3 (added): Given an existing v1 manifest that requests no navigation permission,
  when it is parsed and activated, then it remains valid and its previously
  granted commands, editor, and notes capabilities behave unchanged.

Approach: Keep navigation permissions additive and default-deny. Existing manifests need no migration and existing host capability registration remains unchanged when no navigation callbacks are supplied.

Files:
- `src/script-manifest.ts`
- `src/extension-host.ts`
- `src/script-manifest.test.ts`
- `src/extension-host.test.ts`

## AC-4 (added): Given a navigation call with traversal, a reserved `.brulion` path,
  unsafe path characters, an invalid anchor/options shape, or an unknown link
  kind, when it crosses the host boundary, then it rejects with a bounded
  validation error and no application callback is invoked.

Approach: Reuse the existing note-path normalization and add explicit bounded validators for open options and resolve options. Validate before permissioned callback invocation and preserve bounded RPC error messages.

Files:
- `src/extension-host.ts`
- `src/extension-host.test.ts`
- `src/extension-runner.test.ts`
- `src/note-name.ts`

## AC-5 (added): Given the sandbox bootstrap, when its public `brulion` object is
  inspected, then navigation methods are available only through the documented
  namespace and return/reject JSON-like values without exposing DOM, URL,
  CodeMirror, or File System Access handles.

Approach: Add only the three navigation wrappers to the existing opaque bootstrap object. They call the authenticated peer and do not capture or serialize host objects; bootstrap tests assert the namespace and no new browser escape path.

Files:
- `src/extension-runner.ts`
- `src/extension-runner.test.ts`

## AC-6 (added): Given navigation method metadata, when the contract is rendered or
  downloaded, then it documents least-privilege permissions, canonical paths,
  discriminated results, and the fact that navigation never implicitly creates
  or mutates a note.

Approach: Describe the navigation namespace, permissions, result unions, no-implicit-create rule, and safety limits in the human and machine-readable Authoring Kit. Add contract drift assertions for method ids, permissions, declarations, and docs.

Files:
- `extension-kit/API.md`
- `extension-kit/api-contract.json`
- `extension-kit/brulion-extension.d.ts`
- `src/extension-api-contract.test.ts`
- `src/api-docs.test.ts`

## Verification

- `npm test -- --run`
- `npm run build`
- `npm run e2e -- e2e/extensions.spec.ts`
- `specman validate`

# Sync plan — FEAT-0098 Markdown anchor compatibility audit

Started: 2026-08-11T19:27:33.646Z
Snapshot state: new
Drift summary: 5 added (whole spec)

## AC-1 (added): Given an existing `other.md` with a heading below the fold and an open
  note containing `[go](other.md#section-title)`, when the user follows the
  link, then the existing M32 path opens `other.md` and scrolls to the matching
  heading.

Approach: Reuse the existing M32 browser follow path and keep the standard `.md`
target in the real Chromium fixture; no new resolver or navigation layer is
needed.

Files:
- `e2e/link-anchors.spec.ts` — cross-note Markdown anchor regression coverage.

## AC-2 (added): Given an open note containing `[jump](#section-title)` and a matching
  heading later in that same note, when the user follows the link, then the
  current note stays open and the editor scrolls to that heading.

Approach: Preserve the existing same-note fragment scenario and assert both the
route/active note and the rendered editor scroll outcome through the existing
M32 path.

Files:
- `e2e/link-anchors.spec.ts` — same-note anchor regression coverage.

## AC-3 (added): Given an existing target note without the requested heading, when the
  user follows `[go](other.md#missing-section)`, then the target still opens,
  no error is shown, and no heading scroll is performed.

Approach: Retain the missing-heading navigation case as a no-op scroll assertion;
production behavior is already the M32 contract.

Files:
- `e2e/link-anchors.spec.ts` — missing-heading regression coverage.

## AC-4 (added): Given `[site](https://example.test/page#fragment)`, when the user follows
  it, then the browser receives the complete external URL including
  `#fragment`, with no in-app note resolution or editor scroll.

Approach: Keep the external-link click interception in the existing browser test
and assert the complete URL, proving the anchor splitter is not duplicated or
applied to external destinations.

Files:
- `e2e/link-anchors.spec.ts` — external fragment regression coverage.

## AC-5 (added): Given any of the anchored navigation cases above, when the browser
  completes the interaction, then the set and bytes of the existing `.md` files
  are unchanged and no new note is created.

Approach: Snapshot the relevant OPFS file contents before navigation and compare
them afterward, strengthening the existing count-only check without changing
application code.

Files:
- `e2e/link-anchors.spec.ts` — exact Markdown-byte and note-set assertions.

## Verification

- `npx playwright test e2e/link-anchors.spec.ts`

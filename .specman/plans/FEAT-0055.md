# Sync plan — FEAT-0055 consistent header icon set with lucide and button normalization

Started: 2026-06-24T07:21:17.882Z
Snapshot state: new
Drift summary: 0 added (whole spec)

## AC-1 (added) — Settings gear is a Lucide icon

Approach: Empty the `#open-settings` button in `index.html` (drop the hand-authored
gear `<svg>`/`<path>` from FEAT-0054). In `src/main.ts`, import `createElement` and
`Settings` from `lucide` and inject `createElement(Settings)` into the button (a tiny
local `icon()` helper shared with AC-2). The button keeps id/aria-label/title/wiring.

Files: index.html, src/main.ts

## AC-2 (added) — Sidebar toggle is a Lucide icon

Approach: Remove the `☰` text from `#toggle-sidebar` in `index.html`. In
`src/main.ts`, inject `createElement(PanelLeft)` (same `icon()` helper). The button
keeps id/aria-label/title, `aria-pressed`, and its `Ctrl+\` wiring.

Files: index.html, src/main.ts

## AC-3 (added) — The two icon buttons render at equal height
## AC-4 (added) — The header icons render at the same size

Approach: In `src/styles.css`, size all header icons uniformly with one rule
(`header button svg { width: 1.1rem; height: 1.1rem; display: block }`) and
generalize the centering to every header button (`header button { display:
inline-flex; align-items: center; justify-content: center }`), replacing the
P1-specific `#open-settings` rule. With identical padding + identical icon box, the
toggle and gear buttons resolve to the same height, and both icons to the same size.

Files: src/styles.css

## AC-5 (added) — Button behavior and shortcuts unchanged

Approach: No behavioral change — only the buttons' inner glyph and sizing change.
The settings click handler + `Ctrl/Cmd+,` and the toggle's `wireToggle` + `Ctrl+\`
are untouched in `src/main.ts`; ids/aria-label/title/`aria-pressed` are preserved.
Verified by an e2e click/keyboard check.

Files: (none — verified, no change)

## Tests

- e2e/header-icons.spec.ts (new) — open a folder, then assert: `#open-settings svg`
  and `#toggle-sidebar svg` both present; the `☰` glyph is gone; the two buttons have
  equal bounding-box height (AC-3); the two icons have equal width/height (AC-4); and
  clicking the gear opens the modal while clicking the toggle flips `aria-pressed`
  (AC-5).
- Existing settings-modal/welcome/vim e2e continue to assert `#open-settings svg` and
  the gear opening the modal — unchanged.

## Verification

- `npm run build`
- `npm test -- --run`
- `npm run e2e -- header-icons settings-modal`

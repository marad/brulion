# Sync plan — FEAT-0094 Extension navigation browser coverage and examples

Started: 2026-08-11T10:13:56.849Z
Snapshot state: new
Drift summary: 8 added (whole spec)

## AC-1 (added): Given a real Chromium page with an OPFS vault and an explicitly enabled
  extension, when it calls `getActiveNote`, then the result travels through the
  opaque iframe/RPC boundary and identifies the actual active note.

Approach: Extend the real OPFS extension fixture with a user-invoked command that calls `navigation.getActiveNote`, publishes the discriminated result as a command label, and assert it after the enabled extension activates in Chromium.

Files:
- `e2e/extension-navigation.spec.ts`
- `src/main.ts`

## AC-2 (added): Given existing and missing OPFS notes, when the extension opens them,
  then existing notes switch in the live editor and missing notes return
  `missing` without a newly created file.

Approach: Exercise `openNote` through the extension command/RPC path for an existing heading-bearing note and an absent note, then assert the live editor/active state and inspect OPFS for absence.

Files:
- `e2e/extension-navigation.spec.ts`
- `src/note-controller.ts`

## AC-3 (added): Given a heading anchor and a note with matching and non-matching
  headings, when the extension opens the note, then the browser observes the
  found/not-found anchor result and no markdown bytes change.

Approach: Use one command for a matching heading and one for a deliberately absent slug, publish both adapter statuses, and compare the target OPFS bytes before and after the calls.

Files:
- `e2e/extension-navigation.spec.ts`
- `src/extension-navigation-adapter.ts`

## AC-4 (added): Given a dirty active editor and an external mtime change, when the
  extension requests another note, then the browser observes `conflict`, the
  external bytes remain intact, and the active note does not silently switch.

Approach: Add a browser fixture command that opens a dirty target request while the page mutates the active file through OPFS, then assert the conflict label, active editor identity, and exact externally written bytes.

Files:
- `e2e/extension-navigation.spec.ts`
- `src/note-controller.ts`

## AC-5 (added): Given markdown and wikilink destinations covering relative paths,
  basename/path matches, anchors, missing targets, external URLs, and invalid
  inputs, when the extension resolves them in Chromium, then results match the
  unit contract and resolution causes no navigation or write.

Approach: Register explicit extension commands for markdown, wikilink, external, missing, and invalid targets; inspect the result labels and verify the editor/OPFS bytes remain unchanged.

Files:
- `e2e/extension-navigation.spec.ts`
- `src/extension-navigation.ts`

## AC-6 (added): Given an extension-created note, when the extension awaits
  `notes.create()` and then calls `navigation.openNote()` without a sidebar
  refresh, then the new note opens successfully and the active route/sidebar
  converge on it.

Approach: Keep the create-then-open command in the OPFS fixture, assert the opened result and empty editor, then wait for the new sidebar row and file bytes.

Files:
- `e2e/extension-navigation.spec.ts`
- `src/note-controller.ts`

## AC-7 (added): Given two vaults or a stale runner from a previous vault, when the old
  runner calls navigation after the switch, then the capability fails closed and
  cannot change the new vault.

Approach: Add a host/adapter stale-folder unit test and a Chromium fixture that reloads the vault before invoking the old extension command; assert the old call rejects and the new vault's editor/file remain untouched.

Files:
- `src/extension-navigation-adapter.test.ts`
- `e2e/extension-navigation.spec.ts`
- `src/main.ts`

## AC-8 (added): Given the Authoring Kit examples, when they are inspected and exercised,
  then they declare least-privilege navigation permissions, use the documented
  result unions, and contain no direct DOM, URL, FSA, network, or implicit
  create-if-missing access.

Approach: Add two copyable examples—fixed journal open and resolve-then-explicit-open—with only the permissions each uses; include them in the deterministic kit and assert manifest/source constraints in the kit tests.

Files:
- `extension-kit/examples/open-journal/manifest.json`
- `extension-kit/examples/open-journal/main.js`
- `extension-kit/examples/resolve-and-open/manifest.json`
- `extension-kit/examples/resolve-and-open/main.js`
- `src/authoring-kit.ts`
- `src/authoring-kit.test.ts`
- `extension-kit/README.md`

## Verification

- `npm test -- --run`
- `npm run build`
- `npm run e2e -- e2e/extension-navigation.spec.ts`
- `specman validate`

# M47 — Rich Markdown editor in CodeMirror

M47 changes the editing model without changing the file model. Brulion continues to
read and write ordinary Markdown files, but the primary editor no longer treats
Markdown source as its visible document. It becomes a rich-text projection backed
by a loss-aware Markdown source map, while CodeMirror remains the editor engine.

M46 is a separate extension-API workstream being developed in parallel. M47 must
preserve the public editor/API boundaries that M46 defines (especially raw
Markdown text and UTF-16 source positions), but M46 is not a reason to delay this
editor work or a dependency on its implementation order.

## Design spine

- **CodeMirror stays.** The existing editor engine, history, selection, keymaps,
  Vim integration, DOM-free tests, and editor extensions remain the foundation.
  This is a model change, not a ProseMirror/Tiptap migration.
- **The visible document is rich text.** The CodeMirror document contains the
  text the user sees. Marks and blocks live in a CodeMirror state field/model;
  Markdown delimiters are not invisible characters that the caret must step over.
  A source map relates visible positions and rich nodes to their Markdown source
  ranges.
- **Markdown remains the file format and source of truth.** Loading parses the
  existing file into the rich projection. Saving serializes the model back to
  Markdown. An untouched note round-trips byte-for-byte; untouched source spans
  keep their original spelling, whitespace, delimiter choice, and ordering.
  A user operation may rewrite only the source span it actually changes, using a
  documented canonical form for newly created syntax.
- **Markdown input is a first-class editing path.** Users may still type
  Markdown. A pending marker sequence can remain visible while it is incomplete;
  once a construct reaches its boundary, one transaction removes the delimiters
  from the visible projection and creates the corresponding rich mark/block.
  `**hello** ` must therefore become visible bold `hello` followed by a normal
  space, while serialization retains `**hello** ` in the file.
- **Boundaries are explicit.** A terminating space is the primary boundary for
  inline marks and line prefixes. Enter, Tab, completed closing constructs, and
  blur/save flush pending input where appropriate, so a closing marker at the end
  of a note cannot remain accidentally raw forever. Each construct gets its own
  boundary rule; a generic character replacement pass is not sufficient.
- **Unknown and opaque syntax is safe.** Markdown the model does not understand
  is never silently discarded. Unsupported material remains an opaque/raw island
  with an explicit source-editing affordance. Frontmatter stays opaque and
  byte-preserving; Mermaid, tables, and code blocks have deliberate source/edit
  behavior rather than accidental cursor-line reveal.
- **One user action, one undo unit.** A conversion such as `**hello** ` → bold
  `hello ` is one CodeMirror history transaction. Caret and selection mapping are
  based on the visible projection, while source offsets are derived through the
  map rather than exposed as hidden caret stops.
- **Raw Markdown APIs remain compatible.** Storage, conflict detection, and the
  extension API consume the current serialized Markdown snapshot. Internal rich
  positions are translated at the boundary; public raw-source positions defined
  by M46 are not silently changed into visible-text positions.

## Phase ledger

- **Current phase:** P4 — Links, code fences, tables, frontmatter, and Mermaid.
- **Last completed gate:** P3 `FEAT-0111` review round 11 clean; targeted and
  full Vitest, build, verification, initial seal, and in-sync status passed.
- **Exact next action:** run the exact-base P4 pre-review handoff, then start
  the canonical `/skill:review-until-clean` loop for `FEAT-0112`.

## Review ledger

- **P1 review:** base `5e935f205d72c17c9df1c17bd9ef9f284767638b`, reviewed HEAD
  `39437b2877ed685166e6bab8da5e885c74b49467`, rounds 1–10 clean after fixes.
- **P2 review base SHA:** `7109d694aa1321fd911779009c07c64a02c71ad4`.
- **P2 reviewed HEAD:** round 1 `8d9a6d2bbf31a6567fc9f21e5eb1a2f6555216a5`; round 2
  `94857436117317a6e3bf922b7d37f18917479ad1`; round 3
  `0ba04379f7a1518ea384226cc70d5c0d3dacbd48`; round 4
  `8555a465b4fcefee9a9926cbdc1e342b855aeada`; round 5
  `34c06b58801a07eed634a35ed39243c180a9fa9b`; round 6
  `020089d428a998d1056a8e6f191320d4be0d3c94`; round 7
  `32d4871d99dd1448a4f9df7cd70f333ceb2d3a8a`; round 8
  `fa8405716533ae88ad2caa9e78e57a3fdf88f109`; round 9
  `cef92fbc6a82e3b1c98a2fee4c22f7fe922b2711`; round 10
  `424e10639d82ccee16172c209432f1a74ae563e7`; round 10
  `a4286a495bd9b92efbeb3c9c1c54456feb307317`; round 11
  `301f97ed32ad146461cc611ebb05d75deb7c3c99`; round 12
  `b76c635548c372c662db3be30475455a2fb7a94a`; round 13
  `861ff84d188d7c808d03714fffa0b48933a28910`; round 14
  `b22e9bda4294592fd3aef2e66c6b719ee68ad46f`; round 15
  `c562d2a7bdad975c7c0585e93c733db422e46eac`; round 16
  `cbfbe4f8f12a0928d05180f59751e4332c8365b0`; round 17
  `a114f68f9ad6d6168c6c874d469e22f27849c1df`; round 18
  `c140c595d54cce7f6294108bebc377f273431b5b`; round 19
  `2eca7d96a469d9342ed6229e20b36a4373fd6c3b`; round 20
  `e2d5ac478ac7c9c316207abe1cffed50a17353ca`; round 21 clean
  `87a10148bf2960f4c9b45c28506b34fa10d44941`.
- **Round/status:** P1 clean; P2 rounds 1–20 — findings; round 21 — clean.
- **Findings/dispositions:** round 1 found URL/punctuation-adjacent imports,
  escaped closing delimiters, paragraph-offset classification, rejected-caret
  mapping, reverse selections, opaque mutation, and cross-fragment toggles were
  unsafe. Fixed in `c88c99f`. Round 2 found reverse nested marks, boundary
  whitespace range calculation, and partial-span unwrapping; fixed in
  `ef91302`. Round 3 found an outer-caret toggle could mistake a nested
  delimiter for its wrapper; `fa694f0` rejects that ambiguous case. Round 4
  found URL-host markers, intraword double underscores, and full outer nested
  unwrapping; `1bf72e9` added guards and enclosing-wrapper unwrapping. Round 5
  found triple-mark unwrapping and latest-boundary classification bugs;
  `f97f766` handles combined markers and scans only the latest completed span.
  Round 6 found nested outer-caret matching, right-side URL adjacency, and
  non-EOF EOF classification; `fbc4c16` adds guards. Round 7 found formatting
  punctuation could be rejected by the import context; `4c804fb` permits
  punctuation-only explicitly formatted spans with a regression test. Round 8
  found URL hosts after an earlier valid mark could bypass the left-side guard;
  `68a7341` rejects both left/right URL adjacency with a regression test. Round 9
  found explicit selections inside words were re-imported as raw and URL
  selections were not rejected; `76ada15` separates explicit-edit parsing from
  conservative import parsing, rejects URL selections, and adds Tab/blur/save/
  idempotence coverage. Round 10 found intraword triple underscores and
  underscore-triple unwrapping were unsafe; `952ac20` adds Unicode-safe guards,
  preserves the valid first span, and keeps imported underscore spelling when
  toggling a combined mark. Round 11 found replaceVisible hid completed marker
  pairs before an explicit boundary; `27ca7c5` adds per-line pending input
  state, conservative re-projection, and explicit flush coverage. Round 12
  found opaque special blocks could still classify inline boundaries, pending
  line offsets shifted at exact-start insertion, and flushing one line committed
  unrelated pending lines; `7473aea` adds opaque-context guards, stable offset
  mapping, and scoped pending flush tests. Round 13 found partial underscore
  formatting was re-imported as raw; `b935d3a` allows adjacent delimiters only on
  explicit edited lines and adds underscore partial-span tests. Round 14 found
  formatting inside code and partial combined-mark toggles could expose or
  duplicate Markdown; `450003d` rejects those unsafe operations. Round 15 found
  a whole pending line replaced with plain text retained raw state; `076cfd0`
  clears pending state when the resulting line has no marker and covers rich
  re-entry. Round 16 found punctuation-only context bypassed conservative
  import boundaries; `0d250a8` separates explicit punctuation formatting from
  normal import/input parsing and adds raw-preservation coverage. Round 17 found
  explicit punctuation formatting was not retained across edits; `25e1dbf`
  stores that transient projection state and adds stability coverage. The
  reported nested outer-mark case is covered by the existing regression and
  reproduced as a successful unwrap, so no code change was warranted. Round 18
  found ordinary edits re-enabled word-adjacent parsing and could reinterpret
  untouched intraword markers; `8d4fbcb` retains explicit-adjacent state only
  for formatting operations and adds a discriminating ordinary-edit test.
  Round 19 found a pending marker line followed by a newline was not flushed at
  EOF, blur, or save; `6048a47` adds pending-line-scoped fallback
  classification and regression coverage. Round 20 found formatting one span
  enabled adjacent parsing across the whole line and could reinterpret
  untouched intraword markers; `e2d5ac4` scopes the opt-in to generated
  delimiter starts and adds an unrelated-formatting regression. Round 21 was
  clean: no material assumption, consistency, file-fidelity, or security
  findings remained. The reviewed implementation HEAD was
  `87a10148bf2960f4c9b45c28506b34fa10d44941`.
- **Command/test evidence:** exact-base P2 pre-review passed at rounds 1–21;
  `npm test -- --run` (64 files, 1170 passed, 1 todo), `npm run build`,
  `specman verify FEAT-0110`, and `specman status --verbose` passed. `specman
  seal FEAT-0110` returned already in-sync with nothing to seal.

- **P3 review base SHA:** `2f1ba94cb65bde11c38c48bfcdbdb1d7c345c33e`.
- **P3 reviewed HEAD:** round 1 findings at
  `521bb30f9d7160f34bd670d81336ed61808e4bf0`; round 2 findings at
  `2a4741e950c11d50232f26d4828412dabe0b18b1`; round 3 findings at
  `be166fd3931de756ad3f4f0a05f9ccdacfd6afd4`; round 4 findings at
  `9d03a1c9fd27f2283d2724ef9ef93275c9723b5f`; round 5 findings at
  `bea9fc57695eb27598b77a01555f58800c5d1274`; round 5
  `5a65a4b50953ee8cee9abc53d8a9f6b45f5a8173`; round 6
  `f0dc9a3eae2c859862013a3b18ad19c6443444bc`; round 7
  `f9ecb77`; round 8
  `2e9cd7e7519c1e4996fac040c05483b3de766e39`; round 9
  `768eb87852b4d0da1b83fc694b0781a6bec21424`; round 10
  `452631f810784c08b23eb6953c1dfdaa880c92c7`; round 11 clean at
  `c8423b980d666bb339110bf2cf6a7b71f65c2d5e`.
- **P3 round/status:** rounds 1–10 — findings; round 11 — clean; P3 is ready
  for final verification and sealing.
- **P3 findings/dispositions:** the first fresh self-review found and fixed
  EOF/CRLF block-cursor mapping, collapsed-selection scope, and opaque-line
  guards in `ca92e2f`; edge cases are covered by regression tests. Canonical
  round 1 found a mismatch because the phase text still implied raw heading
  command integration. `e5cb4d7` explicitly defers those legacy raw EditorState
  adapters to P6, adds FEAT-0111 AC-8, and `f2a391d` records the revised plan;
  no premature P6 integration was added. Canonical round 2 found `>quote`/`>`
  accepted without boundary whitespace and empty list Enter retaining its
  prefix; `7e30fe3` requires quote whitespace and removes the empty prefix while
  preserving an existing line ending. Round 3 found interior Enter discarded
  suffix text, duplicated trailing CRLF, and reset P2 transient projection
  state; `dc48943` preserves the full suffix, consumes only an EOF line ending,
  and reprojects pending/explicit marker state through bounded block edits.
  Round 4 found completed `* ` list prefixes were made opaque by the inline
  parser; `fb7d78d` parses block bodies before inline fallback and adds star-list
  boundary/Enter coverage. Round 5 found Enter could split inside an inline
  span and expose delimiters; `ec4797c` moves boundary splits to inline-span
  edges and rejects unsafe interior splits with regression tests. Round 6
  found block indentation dropped explicit adjacent marker state and ordered
  Enter was treated as a paragraph; `0ebae1b` maps markers through prefix
  replacements and initially continues ordered lists with incremented numbering.
  Round 7 identified that this contradicted the file-fidelity rule; `5570ddb`
  removes ordered continuation/Backspace, and `72aeaef` revises the P0/P3 specs
  and records the decision to keep ordered lists source-preserving. Round 8
  found the decision consequence wording overstated ordered-line immutability;
  the matrix permits ordinary Enter source editing, so `DECISIONS.md` now
  distinguishes unchanged ordered markers/prefixes from an allowed newline.
  Round 9 found nested-mark boundary splits could expose raw delimiters and CRLF
  was not inherited on an unterminated final line; `0fe2d36` rejects shared-mark
  interior boundaries and uses the document's dominant line ending. Round 10
  found nested list indentation was dropped on continuation; `d90c76a` retains
  the existing indentation in generated quote/list prefixes. Round 11 was clean;
  the canonical review verified the complete diff, AC-1–AC-8, and no material
  remaining findings.
- **P3 command/test evidence:** targeted tests, `npm test -- --run` (64 files,
  1171 passed, 1 todo), `npm run build`, and `specman verify FEAT-0111` pass
  after the latest fix. The verification plan initially recursed into
  `specman verify FEAT-0111`; the recursive command was removed and committed
  in `b0ac8b3` before verification.

- **P4 review packet:** base `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b`,
  current HEAD `8b53163c360447a2b44bd123d738ba4de0fccce5`; spec `FEAT-0112`.
  Changed paths: `.specman/plans/FEAT-0112.md`, `specs/FEAT-0112-*`,
  `DECISIONS.md`, `milestones/M47.md`, `docs/excavation/rich-special-blocks/*`,
  `src/rich-specials.ts`, `src/rich-specials.test.ts`, `src/rich-markdown.ts`,
  `src/rich-markdown-p4.test.ts`, and the updated rich-markdown regression
  test. Non-negotiables: complete links expose labels but never hidden targets;
  escaped/malformed links and all special-block bodies stay raw; exact UTF-16
  spans and untouched bytes/line endings survive; special-block mutation is
  explicit and stale edits are rejected; no rendered Mermaid/table/frontmatter
  output enters Markdown. Edge matrix: Unicode/emoji and CRLF; escaped and
  nested link delimiters; aliases/anchors/bare URLs and punctuation; incomplete
  or mismatched fences; fence/comment/table overlap; escaped pipes, uneven
  rows, alignment and body-cell edits; leading/non-leading/unclosed
  frontmatter; Mermaid case/whitespace; adjacent blocks, opaque syntax,
  source/visible boundaries, stale nodes, and normal-edit rejection. Explicit
  non-goals: controller/storage integration, navigation/resolution,
  CodeMirror widgets, Mermaid rendering, clipboard/Vim/extensions, metadata
  interpretation, normalization, and browser validation. Status: pre-review
  handoff passed by `npm run workflow:gate -- pre-review --base
  e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b --spec FEAT-0112`; canonical
  review round 1 pending.

- **P4 review handoff evidence:** the exact-base pre-review gate passed at
  `8b53163c360447a2b44bd123d738ba4de0fccce5`; verification-plan recursion and
  derived-artifact checks passed. No review verdict has been claimed yet.

- **P4 review round 1:** canonical read-only reviewer reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `b9b512174e907a1089677549c1d0f80b4d6d70c1`; status **findings**. Accepted
  findings: unmatched/mismatched fences could expose table-shaped body lines;
  nested wikilinks could expose an inner link; URLs inside inline marks lost
  link metadata; angle-bracket destinations were accepted by the scanner but
  rejected by projection. Fixed together in `f34160c`: unmatched frontmatter/
  fence spans now protect downstream scans, nested/escaped link delimiters are
  rejected, URL boundary trimming coordinates with inline marks, and recognized
  link spans are removed before HTML fallback. Discriminating regressions cover
  each class. Focused evidence before the fix: 25 targeted tests passed; review
  probes reproduced all four findings. Follow-up current-HEAD review pending.

- **P4 review round 2:** canonical follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `b91ab726467d30cbb96af20f3796ca23f1ef5287`; status **findings**. Accepted
  findings: escaped wikilink pipes were treated as literal target syntax,
  malformed nested Markdown could expose an inner scanner node, and URLs in
  inline code were retained in the document link list without a link range.
  Fixed together in `4e5fda6`: escaped pipes are rejected, malformed bracket
  lines are not salvaged, and only link nodes represented by rich fragments are
  accepted. Added discriminating tests for all three. Focused evidence:
  25 targeted tests passed after the batch; follow-up current-HEAD review
  pending.

- **P4 review round 3 (final-candidate):** fresh-context canonical reviewer
  reviewed base `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `a6dc04193cd41c58886a73c2adeb70a29bf72199`; status **findings**. Accepted
  findings: URL scanning rejected valid underscore paths and underscore-wrapped
  URLs; an unclosed frontmatter opener with trailing spaces was protected by
  the scanner but not by the projection; the architecture note contradicted
  supported angle destinations. Fixed together in `044fe6e`: URL marker
  handling now distinguishes emphasis boundaries from valid path underscores,
  frontmatter detection shares the scanner's trailing-space rule, and the
  architecture note is aligned. Discriminating regressions cover each class.
  Focused evidence: 25 targeted tests and build passed after the batch.
  A fresh current-HEAD final review is required.

- **P4 review round 4:** fresh-context canonical reviewer reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `c0f8210cc1f315313902e3d256192d36344d56d1`; status **findings**. Accepted
  findings: valid URL paths ending in `_` were trimmed as emphasis, and
  multiline unknown HTML regions did not protect their links/table-shaped
  lines. Fixed together in `2189cc0`: closing emphasis markers trim only when
  an opening marker is present, and shared HTML-block spans protect scanner and
  projection passes. Discriminating tests cover trailing underscores and a
  multiline HTML block. Focused evidence: 25 targeted tests and build passed
  after the batch. A fresh current-HEAD final review remains required.

- **P4 review round 5:** fresh-context canonical reviewer reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `dae99f5760e8de590940e1e77e0df68cc9012999`; status **findings**. Accepted
  findings: consecutive underscores in valid URL paths were rejected, and
  nested HTML tags prematurely ended multiline opacity. Fixed together in
  `7a62ec3`: URL filtering now rejects only actual asterisk/backtick marker
  ambiguity, while HTML spans use a tag stack and are shared by scanner and
  projection. Discriminating regressions cover consecutive underscores and
  nested tags. Focused evidence: 25 targeted tests and build passed after the
  batch. A fresh current-HEAD final review remains required.

- **P4 review round 6:** fresh-context final-candidate reviewer reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `4c21b9ecf6c43022b915a7f436fc9724be6191c8`; status **findings**. Accepted
  findings: forged link/cell metadata could redirect explicit edits across
  nodes; raw source edits could span recognized links/specials; the exported
  link scanner could salvage a URL from a malformed bracket line; and angle
  destinations with surrounding spaces were blocked as HTML. Fixed together in
  `14e3002`: freshness now matches every link/cell coordinate and identity,
  raw edits reject recognized-island overlap, malformed link lines shield the
  URL pass, and angle-destination shielding shares the parser's whitespace
  rule. Discriminating regressions cover all four classes. Focused evidence:
  17 targeted tests and `npm run build` passed after the batch. A retained
  current-HEAD follow-up review remains required.

- **P4 review round 7:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `22f29d69c1fc0edff35550971237220fceea9df1`; status **findings**. The
  exported scanner still leaked a URL from an escaped link-like line because
  the malformed-line guard excluded escaped `[` characters. Fixed in
  `3040624` by shielding every bracketed malformed/escaped line, with a
  discriminating regression for `\\[x](https://x.test/a)`. Focused evidence:
  17 targeted tests passed after the fix. A retained current-HEAD follow-up
  review remains required.

- **P4 review round 8:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `d59336ffb6dfeddaf75787fc6843b0622b87fee1`; status **findings**. The
  escaped-bracket guard had become line-wide and suppressed valid links on
  either side of an escaped literal; the projection likewise discarded the
  whole line. Restructured in `69733b9` to shield only the escaped span and
  project valid links alongside raw islands, with discriminating scanner and
  projection regressions. Focused evidence: 17 targeted tests and
  `npm run build` passed after the batch. A retained current-HEAD follow-up
  review remains required.

- **P4 review round 9:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `c3655ce7b9f5333317da0d1dd5620c2668d0aee4`; status **findings**. Accepted
  findings: mixed projection was enabled for every opaque line, exposing links
  inside unsupported strikethrough/marker/incomplete-HTML syntax and blocking
  raw repair; rejected escaped-pipe wikilinks still shielded the entire line.
  Fixed together in `1909267` by limiting mixed projection to local escaped
  link islands, preserving other opaque lines verbatim, and isolating
  escaped-pipe wikilinks. Discriminating regressions cover unsupported raw
  lines, raw repair, and valid links before/after escaped wiki syntax. Focused
  evidence: 17 targeted tests and `npm run build` passed after the batch. A
  retained current-HEAD follow-up review remains required.

- **P4 review round 10:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `89aadcf8f6b01647df3566765747506787c7309a`; status **findings**. Accepted
  findings: mixed projection could expose links inside inline-code spans, and
  unsupported markers/HTML inside a local escaped island could suppress valid
  links outside it. Fixed together in `0ff7d2b` by sharing local raw-island
  spans between scanner and projection, masking only those spans for the
  adjacency check, and filtering link nodes inside inline code. Discriminating
  regressions cover inline-code protection and raw-island adjacency. Focused
  evidence: 17 targeted tests and `npm run build` passed after the batch. A
  retained current-HEAD follow-up review remains required.

- **P4 review round 11:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `418dddc831460b8e912dbd8eef451a8ee0dd6128`; status **findings**. A
  backtick inside a local raw island could pair with a neighboring backtick
  and incorrectly classify/filter a valid outside link. Fixed in `bfed118` by
  making delimiter scans skip raw spans and by treating only the resulting
  true opaque spans as link blockers. Added a discriminating raw-backtick
  adjacency regression. Focused evidence: 17 targeted tests and
  `npm run build` passed after the batch. A retained current-HEAD follow-up
  review remains required.

- **P4 review round 12:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `f7a4fda655d7de61575e96d75a0c73277be3e3cd`; status **findings**. Accepted
  findings: links inside unclosed emphasis and incomplete HTML remained
  projectable when a local raw island was present, allowing normal mutation
  and blocking raw repair. Fixed in `5bc6e01` by filtering unclosed emphasis
  delimiters and extending shared HTML shielding to incomplete tag lines.
  Discriminating regressions cover both unsupported forms and raw repair.
  Focused evidence: 17 targeted tests and `npm run build` passed after the
  batch. A retained current-HEAD follow-up review remains required.

- **P4 review round 13:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `ac59efbae79d7b30a8ea3b2aa622294e5d4cc268`; status **findings**. Accepted
  finding: mismatched emphasis runs such as `***one**` and `**one***`
  could expose a following link when a local raw island was present. Fixed in
  `4b8e881` by detecting unequal emphasis runs, including short-run fallbacks,
  while preserving valid nested/combined emphasis. Discriminating regressions
  cover the reported marker combinations. Focused evidence: 17 targeted tests
  and `npm run build` passed after the batch. A retained current-HEAD follow-up
  review remains required.

- **P4 review round 14:** retained reviewer follow-up reviewed base
  `e0a59037a5d7bfb63ea5cdc8e88ad5d46fa0005b` at HEAD
  `c3a5319376be648083073ae37d27030494211fd9`; status **findings**. Accepted
  finding: valid nested emphasis such as `*outer **inner** end*` was rejected
  beside a local raw island because the unclosed scan matched inner runs as
  outer closers. Fixed in `ec114cf` by reusing the run-aware delimiter matcher
  and added discriminating regressions for nested/combined emphasis. Focused
  evidence: 17 targeted tests and `npm run build` passed after the batch. A
  retained current-HEAD follow-up review remains required.

## Phases

- [x] **P0 — Rich document contract and Markdown behavior matrix** (`FEAT-0107`)
  Define the rich model, source-map invariants, visible/source position rules,
  serialization ownership, and a behavior matrix for every current Markdown
  construct: paragraphs, headings, bold, italic, inline code, blockquotes,
  unordered/ordered lists, links, wikilinks, fenced code, tables, frontmatter,
  Mermaid, and unknown syntax. For each construct specify recognition, the
  temporary pending state, the boundary that commits it, caret/Enter behavior,
  source editing, and round-trip rules.

  **Done =** the matrix and contracts are specific enough to write discriminating
  tests, the lossless/changed-span policy is explicit, and no construct relies on
  an unrecorded reveal-on-cursor exception.

- [x] **P1 — Loss-aware import and visible rich projection** (`FEAT-0109`)
  Build the CodeMirror state/model layer that parses an existing Markdown note into
  visible text plus rich marks/blocks and source mappings. Start with paragraphs,
  headings, inline marks, blockquotes, lists, inline code, and opaque fallback
  regions. Loading a file must not rewrite it, and the projection must support
  deterministic visible-position/source-position mapping.

  **Done =** existing notes render as rich text without hidden Markdown caret stops,
  unchanged content serializes byte-for-byte, unknown syntax survives as a safe
  raw region, and unit tests cover nested marks, multiline blocks, Unicode, and
  source-map boundary positions.

- [x] **P2 — Inline Markdown input rules and formatting model** (`FEAT-0110`)
  Add user-typing conversion for `**…** ` / `__…__ `, `*…* ` / `_…_ `, and
  `` `…` `` with the agreed boundary behavior. Add the equivalent rich model
  operations behind Ctrl/Cmd shortcuts and selection formatting, preserving the
  marker spelling for imported content and using the canonical form only for new
  syntax. Incomplete or literal markers remain editable instead of being guessed
  away.

  **Done =** typing `**hello** ` produces visible bold text plus an unformatted
  trailing space in one undo step; headings/blocks are not accidentally parsed as
  inline marks; nested marks, empty spans, escaped markers, URLs, punctuation,
  Enter/Tab, EOF, blur, and undo/redo have discriminating coverage.

- [x] **P3 — Block input rules and rich block editing** (`FEAT-0111`)
  Convert line prefixes such as `# `, `## `, `> `, `* `, and `- ` at their
  Markdown boundaries into headings, quotes, and list blocks. Rework Enter,
  Backspace, continuation, and exit against the rich model rather than against
  hidden source characters, and expose mapping-safe block indentation operations.
  Heading-level shortcut/slash command adapters remain a P6 integration concern;
  P3 must not route the current raw-EditorState adapters prematurely. Ordered
  lists follow the revised P0 rule: they remain source-preserving/plain in P3
  without continuation, renumbering, or prefix normalization.

  **Done =** typing a completed line prefix produces the intended rich block with
  correct caret placement, list/quote continuation and exit are predictable,
  block operations no longer insert raw Markdown markers into the visible
  document, and all block source round-trips exactly when untouched. Existing
  raw command adapters are explicitly recorded for P6 rather than silently
  treated as rich-model integration.

- [ ] **P4 — Links, code fences, tables, frontmatter, and Mermaid** (`FEAT-0112`)
  Move the remaining current renderers onto the new model boundary. Complete
  Markdown links and wikilinks into rich link nodes while keeping target editing
  deliberate; enter and leave fenced code blocks through explicit fence/Enter
  rules; define table completion/editing; keep frontmatter opaque and expandable;
  and keep Mermaid as a rendered block with a safe source-editing path. No
  construct may depend on revealing raw syntax merely because the caret entered it.

  **Done =** each supported special construct has a documented rich or opaque
  representation, a deliberate way to edit its source, correct navigation/render
  behavior, and byte-preserving tests for loaded and newly typed examples.

- [ ] **P5 — Loss-aware serialization and note-controller integration** (`FEAT-0113`)
  Replace direct `view.state.doc` Markdown reads with an editor document boundary
  that provides the current serialized Markdown snapshot. Integrate autosave,
  flush, note switching, lazy note creation, rename/move, external refresh, and
  conflict handling. Compare dirty/conflict state using serialized source, and
  preserve visible caret/viewport anchors when external Markdown is reparsed.

  **Done =** every existing storage path saves the correct Markdown, an untouched
  file is byte-identical, only intended changed spans are rewritten, external
  refresh and conflicts never lose rich edits, and note-controller tests cover
  source/model divergence and stale-write guards.

- [ ] **P6 — Selection, clipboard, commands, Vim, and extension compatibility** (`FEAT-0114`)
  Adapt selection toolbar, slash commands, copy/cut, paste, link autocomplete,
  heading anchors, and the M22 boundary-fidelity behavior to visible rich
  selections. Preserve raw Markdown semantics for the extension API, including
  M46's direction-aware source positions, by translating through the source map.
  Audit opt-in Vim mode over the visible projection and keep source editing as an
  explicit mode rather than reintroducing hidden-marker motions.

  **Done =** visible selections format and copy/paste predictably, internal copies
  retain the intended Markdown/rich meaning, extension reads/writes remain
  source-compatible, Vim either works over visible text or has an explicit tested
  limitation, and no integration reintroduces hidden syntax as normal caret text.

- [ ] **P7 — Migration cleanup and full browser validation** (`FEAT-0115`)
  Remove or retire the old raw-Markdown decoration paths that are superseded by
  the rich model, keep CodeMirror-only workbench/diff editors independent, and
  validate realistic editing flows in Chromium/OPFS: typed conversions, existing
  notes, reloads, conflicts, links, special blocks, source editing, undo, and
  byte fidelity. Keep external `text/html` clipboard interoperability separate
  unless the matrix proves it is required for the core editor contract.

  **Done =** the main editor uses one coherent rich projection, obsolete hidden
  marker machinery is no longer load-bearing, full unit/build/E2E validation passes,
  and representative notes remain byte-faithful through load/edit/save/reload.

## Done =

The main Brulion editor feels like a rich-text editor while Markdown remains the
portable file format. Existing Markdown opens as rich content; typing Markdown
markers such as `**hello** ` and `# ` converts them at explicit boundaries; marks,
blocks, links, and special constructs have deliberate editing rules; unknown
syntax is safe; and saving never silently normalizes or drops unrelated source.
CodeMirror, FSA storage, conflicts, extension boundaries, and the M46 raw-source
contract remain intact. The old hidden-Markdown caret model is no longer the
foundation of the editor.

## Out of scope

- Replacing CodeMirror with ProseMirror, Tiptap, or another editor engine.
- A general Markdown reformatter or whole-document canonicalization pass.
- Silent deletion of unsupported Markdown, HTML, frontmatter, or future syntax.
- A full structured frontmatter form or interpretation of metadata fields.
- Arbitrary custom extension UI, DOM exposure, or a new storage/index sidecar.
- External `text/html` clipboard interoperability and HTML-to-Markdown paste as a
  requirement of the first rich-model cut; these remain a coupled follow-up unless
  the P0 behavior matrix demonstrates that the core editor cannot be usable without
  them.
- Changes to FSA permissions, vault identity, sync, or note-file ownership.
